pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                def scannerHome = tool 'SonarQubeScanner3'
                withSonarQubeEnv('SonarQube') {
                    sh "D:/sonar-scanner-4.6.2.2472-windows/bin/bin/sonar-scanner"
                }
            }
        }
    }
}
